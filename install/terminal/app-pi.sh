#!/bin/bash

# Install Pi, a minimal terminal coding harness. See https://pi.dev/docs/latest
# Installs the `pi` command globally. Requires Node.js (provided by mise in terminal.sh).
curl -fsSL https://pi.dev/install.sh | sh
